# java-calculator-precourse

# GOAL

입력받은 문자열을 해석하여 숫자의 합을 구한다.

# TODO

- 테스트 용이성과 기능별 구현을 위해 처리 로직을 세 단계로 나눕니다.
- Reader
    - [x] 입력 값을 받아 구분자를 기준으로 분해하고 결과를 반환합니다.
    - [x] 구분자 처리
        - [x] 사용자 정의 구분자가 존재하는지 확인합니다.
        - [x] 기본 구분자 `,`와 `:`가 있는지 확인합니다.
        - [x] 사용자 정의 구분자가 없는 경우 기본 구분자만을 허용합니다. 잘못된 경우 `Exception case`항목에 정의된 내용을 따릅니다.
    - [x] 입력값 처리
        - [x] 입력된 수는 양수만을 허용합니다.

- Writer
    - [ ] Reader에서 읽은 값을 `결과: %d` 형식으로 콘솔에 씁니다.

- Exception case
    - [x] 모든 예외 케이스의 경우 `IllegalArgumentException`을 던집니다.
